{
  "parameters": {
    "blurb": "resp429 simulates an upstream service that is always rate-limiting you with 429 Too Many Requests. Nothing is forwarded, so you can trigger the response on demand instead of burning a real provider's quota to produce one. Use it to test backoff and retry logic, and to confirm your client slows down rather than hammering an endpoint.",
    "label": "Too Many Requests",
    "status": 429
  },
  "template": "resp"
}
