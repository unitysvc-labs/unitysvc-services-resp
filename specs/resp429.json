{
  "parameters": {
    "blurb": "Because nothing is forwarded, resp429 lets you trigger a rate-limit response on demand instead of burning a real provider's quota to produce one. Use it to test backoff and retry logic, and to confirm your client slows down rather than hammering an endpoint.",
    "label": "Too Many Requests",
    "status": 429
  },
  "template": "resp"
}
