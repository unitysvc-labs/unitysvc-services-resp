{
  "parameters": {
    "blurb": "Because nothing is forwarded, resp503 is a target that is guaranteed to fail. Use it to test how your code handles an unavailable service, and as a deliberately failing service when exercising failover \u2014 the gateway returns 503, so anything watching for a failure will see one.",
    "label": "Service Unavailable",
    "status": 503
  },
  "template": "resp"
}
