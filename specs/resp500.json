{
  "parameters": {
    "blurb": "resp500 simulates an upstream service that always fails with 500 Internal Server Error. Nothing is forwarded, so it is a target guaranteed to fail every time. Use it to test how your code handles a server error, and as a deliberately failing service when exercising failover.",
    "label": "Internal Server Error",
    "status": 500
  },
  "template": "resp"
}
