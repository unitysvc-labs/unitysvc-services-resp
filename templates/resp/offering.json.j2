{
  "name": "resp{{ status }}",
  "service_type": "gateway",
  "capabilities": ["http_relay"],
  "description": "resp{{ status }} is a system service: the gateway stops routing the request to an upstream provider and returns {{ status }} {{ label }} directly.\n\n{{ blurb }} Sending a request to https://api.svcpass.com/resp{{ status }} with your API key returns {{ status }} {{ label }} immediately.",
  "status": "ready",
  "tags": ["gateway", "test", "direct-response"],
  "time_created": "2026-05-31T00:00:00Z",
  "upstream_access_config": {
    "direct_response": { "access_method": "http", "base_url": "resp://{{ status }}" }
  }
}
